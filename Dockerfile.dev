# See Dockerfile for why Node 22.
FROM node:22-alpine

EXPOSE 8000
WORKDIR /app

COPY package.json package-lock.json ./
RUN npm ci

COPY . .

# -H 0.0.0.0 so the dev server is reachable from outside the container.
CMD ["npx", "gatsby", "develop", "-H", "0.0.0.0"]
